配置软件源
sudo pacman-mirrors -i -c China -m rank
sudo pacman -S vim
sudo vim /etc/pacman.conf
[archlinuxcn]
SigLevel = Optional TrustedOnly
Server = https://mirrors.ustc.edu.cn/archlinuxcn/$arch
sudo pacman -Syy && sudo pacman -S archlinuxcn-keyring 
sudo pacman -S zsh fcitx-im fcitx fcitx-sogoupinyin fcitx-configtool git vim clang make cmake python-pip shadowsocks-qt5

安装zsh
sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
chsh -s /usr/bin/zsh

添加输入法配置文件
sudo gedit /etc/environment
sudo vim ~/.xprofile
加入：
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx

Install Shadowsocks:
安装命令行shadowsocks:
sudo pacman -S shadowsocks
配置服务器信息：
sudo vim /etc/shadowsocks/shadowsocks.json
配置信息:
{
"server":"YourServer",
"server_port":YourPort,
"local_address": "127.0.0.1",
"local_port":1080,
"password":"YourPassword",
"timeout":600,
"method":"aes-256-cfb",
"fast_open": false,
"workers": 1,
"prefer_ipv6": false
}
运行shadowsocks:
sslocal -c /etc/shadowsocks/shadowsocks.json
添加快捷方式：
sudo vim ~/.bashrc

alias vpn="sslocal -c /etc/shadowsocks/shadowsocks.json"

浏览器安装插件swtichImege:
配置信息:
127.0.0.1
1080

命令行连接ＷｉＦｉ
ip link     //获取网卡名称wl~~
wpa_passphrase <名称> <密码> > /etc/wpa_supplicant/wpa_supplicant.conf
sed -i '1 i\curl_interface=/run/wpa_supplicant' /etc/wpa_supplicant/wpa_supplicant.conf
sed -i '2 i/update_config=1' /etc/wpa_supplicant/wpa_supplicant.conf
wpa_supplicant -B -i <网卡名称> -c /etc/wpa_supplicant/wpa_supplicant.conf
dhclient <网卡名称>

如果ssh显示broke pipe
example: ssh -o IPQos=0x10 ubuntu@192.168.8.250
